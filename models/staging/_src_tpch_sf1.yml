version: 2

sources:
  - name: tpch_sf1
    database: SNOWFLAKE_SAMPLE_DATA
    schema: TPCH_SF1
    tables:
      - name: customer
      - name: orders
        config:
          freshness:
            warn_after:
              count: 8
              period: hour
          loaded_at_field: "CAST(o_orderdate AS TIMESTAMP)"
      - name: lineitem
        config:
          freshness:
            warn_after:
              count: 8
              period: hour
          loaded_at_field: "CAST(l_receiptdate AS TIMESTAMP)"
      - name: part
      - name: supplier
      - name: partsupp
      - name: nation
      - name: region