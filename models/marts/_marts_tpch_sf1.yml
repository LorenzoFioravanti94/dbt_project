models:
  - name: dim_geography
    columns:
      - name: geography_id
        description: Primary Key
  - name: dim_customer
    columns:
      - name: customer_id
        description: Primary Key
      - name: geography_id
        description: Foreign Key -> dim_geography
        data_tests:
          - relationships:
              field: geography_id
              to: ref('dim_geography')
  - name: dim_priority
    columns:
      - name: priority_id
        description: Primary Key
  - name: dim_part
    columns:
      - name: part_id
        description: Primary Key